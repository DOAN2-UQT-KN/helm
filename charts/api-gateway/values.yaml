# Default values for api-gateway
replicaCount: 1

image:
  repository: uqtri/api-gateway
  pullPolicy: IfNotPresent
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1001
  fsGroup: 1001

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: false

service:
  type: ClusterIP
  port: 80
  targetPort: 8081

ingress:
  enabled: true
  className: "traefik"
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/rate-limit: "100"
  hosts:
    - host: api.ecolink.id.vn
      paths:
        - path: /
          pathType: Prefix
  tls: []
    # - secretName: api-gateway-tls
    #   hosts:
    #     - api.ecolink.id.vn

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 1
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}
tolerations: []
affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - api-gateway
          topologyKey: kubernetes.io/hostname

# publicGatewayUrl: OpenAPI `servers` + Swagger (match ingress).
# corsOrigin: SPA origin for Access-Control-Allow-Origin. Use "*" for permissive CORS.
external:
  publicGatewayUrl: "https://api.ecolink.id.vn"
corsOrigin: "https://ecolink.id.vn"

# Upstream ClusterIP bases (no trailing slash). Internal identity routes use /internal/v1 on identity-service directly, not via the gateway.
internal:
  identityServiceUrl: "http://identity-service:3000"
  incidentServiceUrl: "http://incident-service:3001"
  rewardServiceUrl: "http://reward-service:3002"
  identityInternalRoutePrefix: "/internal/v1"

env:
  - name: NODE_ENV
    value: "production"
  - name: PORT
    value: "8081"

livenessProbe:
  httpGet:
    path: /health
    port: 8081
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /health
    port: 8081
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3
